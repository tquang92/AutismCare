import { StyleSheet } from "react-native"

export const STYLES = StyleSheet.create({
    title: {
        fontSize: 16,
        fontWeight: "700",
        color: "#FFF",
    },
    cardItem: {
        backgroundColor: "#6B7280",
        width: "100%",
        height: 96,
        borderRadius: 16,
        alignItems: "center"
    },
    image: {
        width: 64,
        height: 64,
        borderRadius: 999,
    }
})